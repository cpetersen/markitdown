require 'html-markdown'

describe Html::Markdown do
  context "when parsing nested lists" do
    it "should return valid markdown" do
      pending
    end
  end

  context "when parsing lists nested under blockquotes" do
    it "should return valid markdown" do
      pending
    end
  end

  context "when parsing lists nested under lists under blockquotes" do
    it "should return valid markdown" do
      pending
    end
  end
end
